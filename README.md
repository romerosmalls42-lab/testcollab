# testcollab

A collaborative testing landing page.

## Preview locally

Open `index.html` in your browser, or run a simple server:

```bash
python3 -m http.server 8080
```

Then visit [http://localhost:8080](http://localhost:8080).

## Deploy

This is a static site — host it on GitHub Pages, Netlify, or any static host by serving the repo root.
